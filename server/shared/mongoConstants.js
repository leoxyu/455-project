// Remove the export statements
const DATABASE_NAME = 'unifi';
const USER_COLLECTION = 'users';
const PLAYLIST_COLLECTION = 'playlists';

// Export the variables as an object
module.exports = {
  DATABASE_NAME,
  USER_COLLECTION,
  PLAYLIST_COLLECTION
};